# Open Questions: fix-dependabot-security-alerts

speq-plan-pr could not complete this plan without human input. What's done so far is committed on this branch. Reply inline on the PR, or resume with `/speq:plan fix-dependabot-security-alerts` locally, or re-run `/speq:plan-pr fix-dependabot-security-alerts` after commenting.

- [ ] Task 1.5 dismisses GitHub Dependabot alert #18 (`gh api -X PATCH .../dependabot/alerts/18 -f state=dismissed ...`) so the alert's state matches the `deny.toml` suppression policy. `plan-reviewer` found (round 2) that this is delegated to `git-agent`, but `git-agent`'s exclusive operation catalog (`speq:git-operations`) has no alert-dismiss verb — only branch/commit/push/PR/issue operations. Two ways to close this: (a) execute the dismissal as a manual, human-run step outside `speq-implement`'s automated task flow (documented as such in plan.md, not delegated to git-agent), or (b) add a `dismiss-dependabot-alert` operation to `git-agent`'s catalog as a prerequisite before task 1.5 can run automated. Which do you want — and if (a), should the plan still ship 0.14.1 with task 1.5 marked "manual, not automated," or should the alert dismissal be dropped from this plan's scope entirely and tracked separately?
